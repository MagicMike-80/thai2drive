MONGO_URL="mongodb://localhost:27017"
DB_NAME="test_database"
JWT_SECRET="7b31098abee17615e40a39d70e6d2b9acbe709c555ebf71888fa0b2e86dbc72e"
EMERGENT_LLM_KEY=sk-emergent-b48A3D57008C8350c6

# ── Support chat email ──
SUPPORT_SMTP_HOST=smtp.mail.yahoo.com
SUPPORT_SMTP_PORT=587
SUPPORT_SMTP_USER=Michael.6lee@yahoo.com
SUPPORT_SMTP_PASS=xyajrocvtefueiga
SUPPORT_EMAIL_TO=lexuz.zxc@gmail.com

# ── Custom domain (set 2026-04-23) ──
# Public marketing URL — drives canonical, og:url, sitemap, robots.
# Both thai2drive.no AND www.thai2drive.no serve the same content directly
# (no redirect); this value is the one we tell search engines to prefer.
PUBLIC_SITE_URL=https://thai2drive.no
# After Emergent adds the ingress rewrite for /, /privacy, /terms, /support,
# /sitemap.xml, /robots.txt, flip the line below to "clean" so the /api/
# prefix is dropped from canonical URLs.  Leaving it default ("prefixed")
# is safe in the meantime — URLs just include /api/.
# SITE_ROUTING_MODE=clean
